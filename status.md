# Printing Website — Status & Plan Review

**Purpose of this document:** a plain-language summary of where this project stands and what's planned — for anyone following along without reading the technical specs in detail. Updated as work progresses; the full technical detail behind any item lives in `specs/` once a feature reaches planning, or in [`CHANGELOG.md`](CHANGELOG.md) for a push-by-push history.

**Where things stand:** the app itself exists and runs (Next.js scaffold, database connectivity, CI-checkable), and the project's constitution — the ground rules for how this gets built (scope, quality bar, process) — is **ratified as of 2026-07-07 (v1.0.2)**. **All five MVP features are implemented and verified live** (admin product management, catalog & browsing, cart & checkout, order confirmation, admin orders/discounts/shipping), plus a first post-MVP feature (site content pages — Privacy/Terms/About). See the table below and "What's happened so far" for detail on each. What's left toward an actual launch is pre-launch credential work, not more building — see "Next steps."

---

## At a glance

Build order (decided 2026-07-07): **admin product management ships
first**, so real products can be loaded before any storefront work
begins — everything else follows the customer-facing flow order.

| # | Feature | Status | One-liner |
|---|---|---|---|
| — | Project setup | ✅ Scaffolded | Next.js app, TypeScript/Tailwind/Zod, PostgreSQL via Drizzle, a `/api/health` check, and CI all wired up and passing. |
| 1 | Admin: product management | ✅ Implemented | Auth.js/Google-SSO-gated admin shell + Products list (search, pagination, thumbnails, edit/delete) + Product Editor (variant/pricing config, 1..n images, catalog-driven styling/material) + Categories/Styling/Materials management — enough to load and maintain real products. |
| 2 | Catalog & browsing | ✅ Implemented | A public storefront (browse by category + product detail with live, selectable-option pricing) reading real Active products seeded via #1. Draft products are unreachable by any URL. No cart yet. |
| 3 | Cart & checkout | ✅ Implemented | Cart (add/adjust/remove, always-fresh pricing), an accurate checkout total (real TaxJar tax, flat/Shippo-calculated shipping, at-most-one promotion), and PayPal payment — an order is only ever marked paid once PayPal's webhook signature is verified server-side. Stripe is a planned fast-follow. |
| 4 | Order confirmation | ✅ Implemented | A real confirmation page (line items, breakdown, status timeline, handles a still-verifying payment gracefully) plus a one-time confirmation email via Resend. |
| 5 | Admin: orders, discounts, shipping & fees | ✅ Implemented | Order queue/detail (fulfillment), promotion management, and a flat-rate shipping setting — sequenced with/after checkout since they're not useful until real orders exist. **The MVP is now feature-complete.** |
| 6 | Site content pages | ✅ Implemented | A post-MVP addition: Privacy Policy/Terms of Use/About Us pages linked from the storefront footer, admin-editable via a rich text editor (Tiptap) — no code change needed to update them, same as promotions/shipping settings. |

Deliberately **not** in the MVP (see [`docs/future-work.md`](docs/future-work.md) for the reasoning): a live product customizer / upload-your-own-design / custom-design tool (the priority fast-follow right after MVP), any AI-assisted feature, extra admin roles, inventory tracking, reviews, wishlists, multi-currency, or subscriptions.

---

## What's happened so far

- **2026-07-06** — Scaffolded the Spec Kit project structure (`.specify/`, `.claude/skills/`), drafted the constitution (v1.0.0, not yet ratified), and added the initial docs/config scaffold (ADR template, `future-work.md`, `non-functional.md`, TypeScript/ESLint/Vitest/Playwright config, CI workflow). Decided the database: PostgreSQL, running locally for now ([ADR-0001](docs/adr/0001-postgres-persistence.md)) — hosted/production Postgres is still an open decision.
- **2026-07-06** — Scaffolded the actual Next.js app: `src/app` (App Router), Tailwind v4, a `public/` folder for static assets, and a `/api/health` route that proves the database connection works. Decided the query layer: Drizzle ORM over Prisma ([ADR-0002](docs/adr/0002-drizzle-orm.md)), with an initial migration applied to the local database. Typecheck, lint, unit tests, build, and e2e all pass. See `CHANGELOG.md` for the exact commits.
- **2026-07-07** — Reviewed the brand assets that landed in `Resources/brand/` (logo family, voice/tone doc) for accessibility — the logos themselves check out (contrast, proper `aria-label`s), with one note for later: the light-mode tagline gray is a bit too light to reuse as real body text, only the logo itself is exempt from that rule. Also softened the constitution's accessibility bar (Principle III): WCAG 2.1 AA stays the target, but it's no longer a hard CI-blocking gate — this is a small business's site, not one under compliance exposure that requires it.
- **2026-07-07** — Reviewed `Resources/models/*.md` (mug, shirt, tote, cart) against the MVP boundary. Confirmed the product-customizer/upload-your-own-design deferral stays as-is (fast-follow, not MVP), but expanded the MVP itself to include promotions/discounts, tax-calculation-API-driven sales tax, and calculated/carrier-rate shipping — these were previously (incorrectly) listed as out of scope. Payment provider is now documented as a genuine Stripe-vs-PayPal open choice, not a "tentatively Stripe" default.
- **2026-07-07** — Reviewed `Resources/brand/Ember Design System.html` (a v0.1 token/component mockup) for accessibility; found three color pairings that fail contrast despite the file's own claim of full AA compliance. Accepted all three as known exceptions rather than fixing them now ([ADR-0003](docs/adr/0003-accepted-contrast-exceptions-ember-v0.1.md)) — none are load-bearing to completing an order, consistent with Principle III's target-not-gate accessibility stance.
- **2026-07-07** — Reviewed `Resources/wireframes/Store Pages.html` (browse/product/cart, desktop + mobile). Three more contrast exceptions accepted into ADR-0003 — one of them (a muted-gray filter label/item-count) is the same color failing for the third time now, on more permanent UI than before, so it's flagged as the one to actually fix if it fails a fourth time. Confirmed the wireframe's "Custom" nav link is an intentional placeholder for the deferred customizer feature (noted in `docs/future-work.md`), and confirmed the cart's lack of a promo-code field isn't a gap — that entry likely belongs on a not-yet-designed checkout step.
- **2026-07-07** — A full UI icon set (~48 icons) and refreshed logo exports landed in `Resources/brand/assets/`. Reformatted `Resources/products/productinfo.md` (equipment lineup + a huge product-category list — apparel, drinkware, home decor, weddings, jewelry, office, promo, restaurant, event, pet, kitchen, holiday, crafts, leather, acrylic, wood, metal, UV-printed, electronics) into proper markdown for readability. Note: this reveals Erica's actual product range is far broader than the handful of example products (shirts/mugs/wood signs/totes) used in wireframes so far — worth keeping in mind once the catalog feature is actually specced, since "fixed catalog" scope discipline (Principle IV) will matter more with dozens of potential categories, not four.
- **2026-07-07** — Reviewed `Resources/wireframes/Admin Screens.html` (orders queue, order detail, products list, product editor, discounts, shipping & fees). The recurring muted-gray contrast issue hit table headers and price-field currency symbols this time — core UI, not placeholders — so it's now actually fixed rather than excepted again: [ADR-0004](docs/adr/0004-darken-muted-text-token.md) darkens the token to `#6B6560` (already used elsewhere in the palette, verified AA-safe everywhere). Also clarified: the admin Product Editor's "Bring your own design"/"Custom design service" pricing config is fine to build as MVP scope (it's just data entry); only the customer-facing upload/ordering flow stays deferred (noted in `docs/future-work.md`).
- **2026-07-07** — Decided the payment provider for real: **PayPal for MVP**, Stripe deferred to a fast-follow ([ADR-0005](docs/adr/0005-paypal-for-mvp-payments.md)) — resolves the long-open Stripe-vs-PayPal question the admin wireframe's dual-processor panel raised again. Updated the constitution, `docs/non-functional.md`, `docs/future-work.md`, and `.env.example`/`.env.local` (Stripe placeholder vars swapped for PayPal ones) to match.
- **2026-07-07** — Formatted `Resources/shared/privacy.md` and `Resources/shared/terms and condition.md` into markdown and swapped the business name in — both were drafted from a generic template for an unrelated business (a digital pet-portrait commission studio), so the *substance* still doesn't match Erica Burns Things (wrong refund policy, wrong jurisdiction, digital-art-specific clauses). Logged in `docs/future-work.md` as needing a real rewrite before these go live; a quick name-swap pass was the deliberate choice for now.
- **2026-07-07** — Reviewed `Resources/products/Launch Catalog.html`: a proposed 22-SKU, 5-category day-one launch list ($12–54, 14 made-to-order/8 stockable) with 2 more categories held for a phase-2 drop. The numbers check out and it's consistent with everything modeled so far. Clarified in the constitution that "stockable" is an internal fulfillment label only, not customer-facing inventory tracking (no scope change). Flagged one real issue in `docs/future-work.md`: the Recipe Board's "handwriting-to-engrave" variant is confirmed to be a customer photo upload, which conflicts with the deferred custom-design boundary — needs resolving before the catalog is finalized.
- **2026-07-07** — Reviewed `Resources/wireframes/Checkout & Confirmation.html` — closes the wireframe gap flagged earlier. Strong overall: promo code confirmed to live on checkout (as guessed), full subtotal/discount/processing/shipping/tax/total breakdown, custom-text personalization step, and a real order-confirmation timeline all match what's already decided. One real conflict found and resolved: the payment section showed "Credit / debit card — via Stripe" as the default method with PayPal secondary — the opposite of ADR-0005. Confirmed this is stale wireframe content, not a reconsideration: checkout builds PayPal-only for MVP (see ADR-0005's Update section).
- **2026-07-07** — Decided the Google SSO implementation: **Auth.js**, over Clerk ([ADR-0006](docs/adr/0006-authjs-for-google-sso.md)) — a managed auth provider was more than the actual need (exactly two known Google accounts gating one admin area). Added `AUTH_SECRET` to `.env.example`/`.env.local`.
- **2026-07-07** — Decided the build order: admin product management ships first (Auth.js shell + Products list + Product Editor), specifically so real products can be loaded before any storefront work begins. The rest of admin (order queue, discounts, shipping & fees settings) moves to sequence with/after checkout instead, since none of it is useful until real orders exist. Updated the feature table above to match.
- **2026-07-07** — **Ratified the constitution, v1.0.0.** Closed out the Sync Impact Report's drafting narrative (the day-and-a-half of refinement is preserved in `CHANGELOG.md`/this file, not repeated in the constitution itself) and updated the version/ratified date. All six ADRs, the expanded MVP scope, and the softened accessibility bar are now the settled ground rules, not a moving draft.

- **2026-07-07** — Ran `/speckit-specify` for feature 1: [specs/001-admin-product-management/spec.md](specs/001-admin-product-management/spec.md). Four prioritized user stories (create a product P1, view the products list P2, edit an existing product P3, duplicate a product P4), 13 functional requirements, 5 measurable success criteria. No `[NEEDS CLARIFICATION]` markers needed — the ratified constitution, accepted ADRs, and reviewed wireframes/product models already covered every ambiguous point with a documented, informed default. Quality checklist passed clean on the first pass.
- **2026-07-07** — Ran `/speckit-plan` for feature 1: [plan.md](specs/001-admin-product-management/plan.md), [research.md](specs/001-admin-product-management/research.md), [data-model.md](specs/001-admin-product-management/data-model.md), [contracts/actions.md](specs/001-admin-product-management/contracts/actions.md), [quickstart.md](specs/001-admin-product-management/quickstart.md). One new ADR owed: `docs/adr/0007-product-options-schema.md` (relational tables per option category — processing/styling/material/size/color/design-location — over a generic key-value table or a JSON blob), to be authored during `/speckit-tasks`/implementation. Prices are integer cents throughout, and price adjustments can be negative (the wireframe shows real discounts, e.g. children's sizing at −$4.00, not just upcharges). Constitution Check passed with no violations, both before and after design.
- **2026-07-07** — Provisioned real Vercel infrastructure: **Neon** Postgres and **Vercel Blob** storage, both via the Vercel Marketplace, connected to the `ericaburnsthings` project's Production/Preview environments ([ADR-0008](docs/adr/0008-neon-for-hosted-postgres.md); closes the constitution's "choose hosted Postgres provider" follow-up, constitution v1.0.1). Verifying the connections surfaced a real bug: `src/db/index.ts` connected to Postgres (and validated `DATABASE_URL`) eagerly at module import time, which crashed every Production build — Marketplace-provisioned secrets are runtime-only and aren't present during `next build`'s page-data collection step. Fixed by making the client connect lazily on first query. Confirmed end-to-end: a fresh Production deployment builds successfully and `GET /api/health` on the live deployment returns `{"status":"ok","db":"connected"}`. Vercel Blob was provisioned but not fully connected at first — no `BLOB_READ_WRITE_TOKEN` existed (only `BLOB_STORE_ID`/`BLOB_WEBHOOK_PUBLIC_KEY`). Fixed via the Vercel CLI (`vercel integration-resource disconnect` then `connect`, scoped to Production/Preview), which provisioned the token correctly. Verified with a real upload/fetch/delete round-trip against the live store — both pieces of Vercel infrastructure are now confirmed working end-to-end.

- **2026-07-07** — Ran `/speckit-tasks` for feature 1: [tasks.md](specs/001-admin-product-management/tasks.md). 31 tasks across Setup, a 12-task Foundational phase (schema/migration, Auth.js + allow-list + a test-only sign-in provider for e2e, pricing/rate-limit/validation helpers, category actions), and the four user stories in priority order (US1 create — the MVP slice — through US4 duplicate), plus a Polish phase (quickstart validation, an accessibility spot-check, the full check suite, and this status/changelog update). US1 is independently shippable as the MVP; later stories reuse its Product Editor component and build outward.

- **2026-07-07** — Caught a real gap before starting feature 2: feature 1's data model had no product-image field at all, despite Vercel Blob having been provisioned specifically for that purpose. Amended feature 1 (`spec.md`, `data-model.md`, `research.md`, `contracts/actions.md`, `plan.md`, `tasks.md`) to add 1..n images per product — attach/reorder/remove in the Product Editor, uploaded server-side via a Server Action (`docs/adr/0009-vercel-blob-for-product-images.md`), duplicated independently alongside the rest of a product's configuration. `tasks.md` grew from 31 to 37 tasks with clean renumbering (nothing had been implemented yet, so no cost to reordering). Constitution bumped to v1.0.2 (patch) to record Vercel Blob as the file-storage choice alongside Neon.

- **2026-07-07** — Ran `/speckit-specify` for feature 2: [specs/002-catalog-browsing/spec.md](specs/002-catalog-browsing/spec.md). Two prioritized user stories (P1 browse the catalog by category, P2 view a product and configure it to see an accurate price), 10 functional requirements, 5 measurable success criteria. No `[NEEDS CLARIFICATION]` markers — the one real ambiguity (what happens to processing options that need the still-deferred customer design-upload flow) already had an informed default from feature 1's own scoping: exclude them from customer selection (FR-006) rather than offer a broken choice. Zero new persisted entities — this feature is a read-only layer over feature 1's data, filtered to Active products only. Quality checklist passed clean on the first pass.

- **2026-07-07** — While planning feature 2, caught two more small cross-feature gaps and amended feature 1 before they could cause rework later: (1) processing options had no way to mark themselves "not yet customer-orderable" (feature 1 gains FR-016 + a `requiresCustomerUpload` flag, so feature 2 can filter on a real flag instead of guessing from label text), and (2) the shared pricing calculation is moved from `src/lib/admin/pricing.ts` to `src/lib/pricing.ts` since it's no longer admin-only. Both are zero-cost since neither feature has been implemented yet.
- **2026-07-07** — Ran `/speckit-plan` for feature 2: [plan.md](specs/002-catalog-browsing/plan.md), [research.md](specs/002-catalog-browsing/research.md), [data-model.md](specs/002-catalog-browsing/data-model.md) (introduces zero new tables — pure read layer over feature 1), [contracts/queries.md](specs/002-catalog-browsing/contracts/queries.md), [quickstart.md](specs/002-catalog-browsing/quickstart.md). One new ADR owed: `docs/adr/0010-catalog-rendering-strategy.md` (dynamic/SSR rendering over ISR, so a product can never keep showing after being flipped to Draft). A new `(storefront)` route group keeps the real branded shell from ever wrapping `/admin`'s separate layout. Constitution Check passed with no violations.

- **2026-07-07** — Ran `/speckit-tasks` for feature 2: [tasks.md](specs/002-catalog-browsing/tasks.md). 17 tasks — a lean Setup+Foundational (an ADR, a placeholder image asset, real branding metadata, and the storefront route-group shell), then the two user stories in priority order (US1 browse is the independently-shippable MVP slice; US2 adds product detail + accurate live pricing, reusing feature 1's pricing function outright), and a Polish phase that also folds a concrete LCP target back into `docs/non-functional.md`'s previously-TBD row. No new database tables and no new Server Actions anywhere in this feature — it's a pure read layer.

- **2026-07-07** — Caught one more feature 1 gap while scoping feature 3 (cart & checkout): the constitution already commits to calculated/carrier-rate shipping for MVP, but `Product` had no weight or package dimensions — the data a shipping-rate API needs to quote a real rate. Amended feature 1 (FR-017, `data-model.md`, `contracts/actions.md`, `tasks.md`) to add optional `weightOz`/`lengthIn`/`widthIn`/`heightIn` fields — one packaged value per product, not per option/size, which is accurate enough at this business's scale.

- **2026-07-07** — Ran `/speckit-specify` for feature 3: [specs/003-cart-checkout/spec.md](specs/003-cart-checkout/spec.md). Three prioritized user stories (P1 build/review a cart, P2 an accurate checkout total with promo/tax/shipping, P3 pay via PayPal with a webhook-verified paid order), 15 functional requirements, 5 measurable success criteria, zero `[NEEDS CLARIFICATION]` markers. Finalizes the tax provider as **TaxJar** (the constitution's prior "leaning TaxJar" note, now a real decision since checkout needs an actual number). Explicitly scopes out admin promotion/shipping/tax configuration UI and the customer-facing confirmation page — both separate, later features (5 and 4). Key Entities flag a real Cart-vs-Order distinction (live-recomputed vs. frozen historical snapshot) for planning to pick up. Quality checklist passed clean on the first pass.

- **2026-07-07** — Ran `/speckit-plan` for feature 3: [plan.md](specs/003-cart-checkout/plan.md), [research.md](specs/003-cart-checkout/research.md), [data-model.md](specs/003-cart-checkout/data-model.md), [contracts/actions.md](specs/003-cart-checkout/contracts/actions.md), [quickstart.md](specs/003-cart-checkout/quickstart.md). Closed two long-open constitution follow-ups for real: **TaxJar** for sales tax and **Shippo** for calculated/carrier-rate shipping (picked after checking how costly a later provider swap would be — contained, since the integration lives behind one small interface). Also decided the cart itself is a client-held cookie reference, never a server table (nothing to persist until an order exists), and that PayPal is integrated via direct REST calls rather than its SDK. Four new ADRs owed (0011–0014). Order line items deliberately snapshot as JSON at time of purchase — the one considered exception to this project's usual typed-table preference, since a paid order is a historical record, not live data. Constitution Check passed with no violations.

- **2026-07-07** — Ran `/speckit-tasks` for feature 3: [tasks.md](specs/003-cart-checkout/tasks.md). 32 tasks — Setup (dependencies + 4 ADRs), a substantial Foundational phase (schema, the checkout rate limiter, the cart-cookie helper, and all four external-integration modules — tax, shipping, promotions, PayPal — each with a deterministic test fake from the start), then the three user stories in priority order (US1 cart is the independently-shippable MVP slice), and a Polish phase that also folds a concrete checkout-step-transition target back into `docs/non-functional.md`. Explicitly noted: this is the first feature whose tasks depend on features 1–2 being *implemented*, not just planned, since it imports their code directly (`src/lib/pricing.ts`, feature 2's product-availability logic).

- **2026-07-07** — Ran `/speckit-specify` for feature 4: [specs/004-order-confirmation/spec.md](specs/004-order-confirmation/spec.md). Three prioritized user stories (P1 an accurate confirmation page right after paying, including graceful handling of a still-verifying payment; P2 a one-time confirmation email once paid, via **Resend** — confirmed with Jon before drafting; P3 the page working correctly on a later revisit via a saved link), 12 functional requirements, 4 measurable success criteria, zero `[NEEDS CLARIFICATION]` markers. Flags the confirmation URL's own unguessability as a real security requirement, not cosmetic, since no customer account system exists anywhere in this project. Adds no new core entity — just a small "email already sent" flag on feature 3's Order. Quality checklist passed clean on the first pass.

- **2026-07-07** — Ran `/speckit-plan` for feature 4: [plan.md](specs/004-order-confirmation/plan.md), [research.md](specs/004-order-confirmation/research.md), [data-model.md](specs/004-order-confirmation/data-model.md), [contracts/actions.md](specs/004-order-confirmation/contracts/actions.md), [quickstart.md](specs/004-order-confirmation/quickstart.md). One new ADR owed: `docs/adr/0015-resend-for-transactional-email.md`. Amended feature 3's `orders` table (before it's implemented, so zero cost) to add `confirmationToken` — a dedicated random public identifier, since this project's IDs are otherwise sequential and FR-012 requires the confirmation URL be unguessable — and `confirmationEmailSentAt`, for the one-time-send guarantee. The brief "confirming payment" window is handled with simple client polling (2s interval, 60s timeout), not push/WebSockets — judged unjustified complexity for a state that resolves in seconds. No new database table. Constitution Check passed with no violations.

- **2026-07-07** — Ran `/speckit-tasks` for feature 4: [tasks.md](specs/004-order-confirmation/tasks.md). 15 tasks — a light Setup (the `resend` dependency + its ADR), a one-task Foundational phase (`getOrderConfirmation`, shared by US1 and US3), then the three user stories in priority order (US1 the confirmation page + confirming→paid polling is the independently-shippable MVP slice; US2 the one-time email, hooked into feature 3's webhook handler; US3 a thin dynamic-rendering + revisit/privacy test on top of US1's page), and a Polish phase. Smallest task count of the four MVP features so far — no new schema, one Server Action, one page, one email module.

- **2026-07-07** — Ran `/speckit-specify` for feature 5: [specs/005-admin-orders-discounts/spec.md](specs/005-admin-orders-discounts/spec.md). Three prioritized user stories (P1 work the order queue through to fulfillment — the daily-driver task; P2 create/manage discounts and promotions; P3 set the flat shipping rate), 13 functional requirements, 5 measurable success criteria, zero `[NEEDS CLARIFICATION]` markers. This is the last MVP feature. Confirms the fulfillment state machine is strictly one-forward-step-at-a-time (`placed`→`paid` stays webhook-only, admin drives `paid`→`in production`→`shipped`), that promotions are entirely admin-CRUD'd here for the first time (feature 3 only read/applied them), and that "shipping & fees" means exactly one setting — the flat-rate shipping amount — since tax and calculated shipping stay fully dynamic via TaxJar/Shippo. Quality checklist passed clean on the first pass.

- **2026-07-07** — Ran `/speckit-plan` for feature 5: [plan.md](specs/005-admin-orders-discounts/plan.md), [research.md](specs/005-admin-orders-discounts/research.md), [data-model.md](specs/005-admin-orders-discounts/data-model.md), [contracts/actions.md](specs/005-admin-orders-discounts/contracts/actions.md), [quickstart.md](specs/005-admin-orders-discounts/quickstart.md). Zero new ADRs owed — every underlying technology (database, Auth.js, admin rate limiter) was already decided by features 1/3. Amended feature 3's (not-yet-implemented) schema again: `orders.status`'s enum now includes `in production`/`shipped` from the start, and `orders.promotionId`'s foreign key now explicitly specifies `ON DELETE SET NULL` — both zero-cost, both close a gap that would otherwise need a real migration later. Order status transitions are validated against an explicit allowed-transition map (`paid`→`in production`→`shipped`, one step at a time), directly implementing Principle II's "explicit, validated state machine" requirement. Adds one new table, `shop_settings` (a deliberate single-row table, not a generic key-value store, for the one flat-rate-shipping setting this project currently has). This feature's Polish phase also finally satisfies Principle V's long-standing "one Playwright happy-path e2e covering the full vertical slice" requirement — no earlier feature could complete it, since the admin queue that test's last step depends on didn't exist until now. Constitution Check passed with no violations.

- **2026-07-07** — Ran `/speckit-tasks` for feature 5: [tasks.md](specs/005-admin-orders-discounts/tasks.md). 21 tasks — a nearly-empty Setup (no new dependencies or ADRs), a 3-task Foundational phase (the `shop_settings` table), then the three user stories in priority order (US1 order queue/fulfillment is the independently-shippable MVP slice; US2 promotion CRUD; US3 the flat shipping rate — deliberately thin, matching its own P3 rationale), and a Polish phase. While drafting this feature's tasks, caught and fixed a real gap in feature 3's own tasks.md: it never actually created the Playwright e2e task its `plan.md` had committed to (features 1, 2, and 4 each have one; feature 3 didn't) — added `e2e/cart-checkout.spec.ts` as feature 3's new T028, renumbering its Polish phase (T028–T032 → T029–T033). Feature 5's own Polish phase adds `e2e/full-vertical-slice.spec.ts` — the single happy-path end-to-end test Constitution Principle V has required since ratification, finally completable now that the admin queue exists.

**All five MVP features are now specified, planned, and tasked.** This closes out the plan-all-before-implement phase of this project.

- **2026-07-07** — Pre-implementation review caught a real gap: `.github/workflows/ci.yml` had no Postgres access at all (no service container, no `DATABASE_URL`), yet `tests/db/connection.test.ts` and `e2e/health.spec.ts` already assert a live, connected database — both CI jobs were at risk of failing (or already were) the moment either ran. Fixed by adding a `postgres:16` service container plus a `DATABASE_URL` env pointing to it, and an `npm run db:migrate` step before tests, on both the `quality` and `accessibility` jobs. Caught now, before feature 1 adds real schema and substantially more DB-backed tests.

- **2026-07-07** — Implemented feature 1 (admin product management), all 37 tasks: Auth.js v5 (Google provider + a test-only Credentials provider for deterministic e2e sign-in), the `categories`/`products`/six option tables/`product_images` schema and migration, the admin rate limiter, the Vercel Blob image-upload wrapper, and the full Products list + Product Editor (create/edit/duplicate, image upload/reorder/remove, an inline category-add, a live example running total). Authored `docs/adr/0007-product-options-schema.md` (0009 was already written in an earlier session). Three real bugs found and fixed along the way: (1) `router.push()` immediately followed by `router.refresh()` raced and could leave the save flow stuck on the old page — removed the redundant refresh; (2) passing an inline arrow function from the edit page's Server Component to the Client Component `ProductEditor` violated the Server/Client boundary (Next.js only serializes direct Server Action references or `.bind()` results) — fixed via `updateProduct.bind(null, productId)`; (3) an invalid option row (e.g. a missing label) failed validation silently, with no field error ever shown — added a general field-error summary so nothing is silently dropped (FR-011/FR-012). An ad hoc axe scan across all three admin pages found and fixed three real accessibility gaps (unlabeled weight/dimension fields, unlabeled option-row inputs, an unlabeled file-upload input, an empty table header) — zero violations on re-scan. All four required checks (`typecheck`, `lint`, `test`, `test:e2e`) pass, plus a production build verified both with and without `DATABASE_URL`/`BLOB_READ_WRITE_TOKEN`/Auth.js env vars present (mirroring ADR-0008's build-time gotcha check).

- **2026-07-07** — Verified feature 1 live in Production. The push that landed it auto-deployed via Vercel's Git integration, but a check afterward found `/api/auth/signin` returning a 500 — `AUTH_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, and `ADMIN_ALLOWED_EMAILS` had only ever been added to local `.env.local`, never to Vercel's actual Production/Preview environment variables. Added all four to both environments (a fresh, distinct `AUTH_SECRET` per environment rather than reusing the local dev one; Google OAuth client ID/secret and the allow-list reused as-is, since it's the same registered Google app across environments) and redeployed. Confirmed live: `/api/auth/signin` now returns 200 and correctly renders only "Sign in with Google" (the test-only Credentials provider stays off in Production, as designed — it's gated on `E2E_TEST_AUTH`, which is never set outside Playwright's own webServer).

- **2026-07-07** — Jon reported two real bugs after signing in on the live site. (1) Signing in from `/admin/products` bounced to the homepage instead of back to the admin page — the layout's `redirect("/api/auth/signin")` never passed a `callbackUrl`, so Auth.js fell back to its default. Fixed with `src/proxy.ts` (Next.js 16's current convention — `middleware.ts` is now deprecated), which redirects unauthenticated `/admin/*` requests to sign-in with the actually-requested path as `callbackUrl`; the layout's own session/allow-list check remains as defense in depth. (2) `/admin/products` and `/admin/products/new` both 500'd — Vercel's runtime logs showed `relation "categories" does not exist`: feature 1's migration had only ever been applied to local Postgres, never to the production Neon database, and `/api/health`'s `select 1` check never would have caught that gap. Since Neon's `DATABASE_URL` is a Marketplace "Sensitive" secret (unavailable at build time, unreadable via `vercel env pull`), there was no way to run `drizzle-kit migrate` against Production from a local machine. Added `POST /api/admin/migrate` — an internal ops route, gated by a random `MIGRATE_SECRET` compared in constant time, that runs Drizzle's `migrate()` against the real runtime connection — plus a `next.config.ts` `outputFileTracingIncludes` entry so the `drizzle/*.sql` files actually get bundled into that route's deployed function. Deployed, ran it against Production, and verified live: the redirect now carries the correct `callbackUrl`, `/admin/products` no longer 500s, and re-running the migrate endpoint is safely idempotent (confirms already-applied migrations are tracked correctly).

- **2026-07-07** — Jon found two more real bugs testing live. (1) The base price field was nearly impossible to type into — `value={(cents/100).toFixed(2)}` reformats to a fixed-decimal string on every keystroke, fighting the user mid-type. Replaced all four currency inputs (base price + the three option-row price-adjustment fields) with a shared `PriceInput` component that keeps its own local text buffer and only reformats on blur; verified with actual character-by-character typing (not Playwright's `.fill()`, which bypasses this exact bug). (2) Adding an image after saving a product opened the file picker but "never updates" — root-caused to a missing try/catch: image upload/removal/reorder had no error handling, so any thrown error (e.g. a Blob upload failure) left the UI stuck in a loading state forever with zero feedback. Added try/catch with a visible error message client-side, plus server-side error handling in `addProductImage` so a Blob failure returns a specific, actionable message instead of Next's generic obfuscated production error. Deployed; the price fix is confirmed working. The image-upload fix needs Jon to retry live — if it still fails, the now-visible error message should finally show the real cause.

- **2026-07-07** — The previous fix made the image-upload failure visible instead of silent, and that surfaced the real cause: `Error: Body exceeded 1 MB limit` (Vercel runtime logs). Next.js caps Server Action request bodies at 1MB by default — `addProductImage` uploads a file as `FormData` through a Server Action, so any real photo (routinely several MB from a phone) was rejected by the framework itself, before the action code (and its try/catch) ever ran. Raised `experimental.serverActions.bodySizeLimit` to `10mb` in `next.config.ts`, and added a matching client-side file-size check so an oversized file shows a clear, specific message instead of hitting the limit. Deployed and confirmed live (health check + build both green); awaiting Jon's retest with a real photo.

- **2026-07-07** — Image upload confirmed working live. Jon's follow-up feedback: the plain browser file input ("Choose File / No file chosen") didn't look like a button, and the "No file chosen" text misleadingly stuck around even after a successful upload (the image already showed in the gallery above it). Replaced with the standard accessible pattern — a visually-hidden file input paired with a styled `<label>` acting as the button ("+ Add photo" / "Uploading…"). Verified locally: correct button text, no leftover native-input text, and clicking the label correctly opens the file chooser. Deployed and confirmed live.

- **2026-07-08** — Before moving to feature 2, Jon asked for five more admin capabilities based on actually using feature 1 live: a dedicated Categories management page; styling and material converted from free-text-per-product to shared, admin-managed catalogs picked via dropdown (with an inline "create new" shortcut), each with their own `/admin/styling`/`/admin/materials` management page (`docs/adr/0016-styling-material-shared-catalogs.md`) — price stays per-product; search (name/description) and pagination (20/page) on the products list; a thumbnail, explicit Edit link, and a real hard-delete button on each list row (reverses feature 1 spec's original no-hard-delete assumption — spec.md's Assumptions amended accordingly); and the site favicon set from the brand seal mark. Schema reshape done as a two-step migration to sidestep drizzle-kit's interactive rename-detection prompt (no TTY available). Deploying surfaced one real gap the local-only verification missed: Production already had a few `styling_options`/`material_options` rows from Jon's own live testing, left with `NULL` catalog references after the additive first step — the finalizing migration's `SET NOT NULL` correctly rejected them. Fixed by deleting orphaned rows (no catalog entry exists for their old free-text label anyway) before finalizing, consistent with this feature's "clean reshape, not a data migration" approach. Verified thoroughly, locally and live: category/styling/material CRUD, cascade vs. set-null delete behavior, a product built via dropdown with per-product pricing persisting correctly on reload, pagination and search across 31 seeded test products, product hard-delete (row + Blob images gone), and all pages correctly gated. Full check suite (`typecheck`/`lint`/`test`/`test:e2e`, 5 e2e tests) and a production build all pass.

- **2026-07-08** — Implemented feature 2 (catalog & browsing), all 17 tasks. Replaced the placeholder homepage with a real, branded storefront (`(storefront)` route group with its own layout — nav/logo/footer — kept separate from `/admin`'s layout): a Browse view grouping every Active product by category (thumbnail, name, starting price, a category filter, a placeholder graphic for products with no photo yet), and a Product Detail view (all images, full description, every configured option category as selectable pills, a live running total reusing feature 1's exact `calculateTotalCents`). Both pages render dynamically (SSR, no ISR/static generation) so a product flipped to Draft is never reachable even for a moment — `docs/adr/0010-catalog-rendering-strategy.md`. A Draft product's detail URL and a nonexistent product's both resolve identically to a branded "not found" page (`(storefront)/not-found.tsx`), with no product data ever leaking. Processing options requiring the still-deferred customer-upload flow (`requiresCustomerUpload`) are excluded server-side in the query layer itself, never client-side only. New `src/lib/catalog/{queries.ts,processing-options.ts}` — a pure read layer, zero new tables, zero new Server Actions. Vitest covers the processing-option filter; a new Playwright e2e (`catalog-browsing.spec.ts`) covers browse → product detail → select/deselect a priced option → exact total, plus both not-found cases. Updating the homepage broke the pre-existing `health.spec.ts` "home page loads" assertion (it checked for the old placeholder heading) — fixed to check the real Browse view instead. An ad hoc axe scan of both new page types found zero violations. Folded the feature's LCP target (<2.5s) into `docs/non-functional.md`'s previously-TBD row. Full check suite (`typecheck`/`lint`/`test` — 15 tests/`test:e2e` — 8 tests) and a production build all pass; the build output confirms `/` and `/products/[id]` are both server-rendered on demand (ƒ), not statically prerendered.

- **2026-07-08** — Erica (the business owner) tried creating products live and they never appeared anywhere — not the storefront, not even as Draft in `/admin/products`. Root-caused to a real, systemic gap: every admin write handler across the app (Product Editor's save + its inline category/styling/material "create new" shortcuts, the Categories/Styling/Materials manager pages, and the products list's Delete/Duplicate buttons) called its Server Action with no `try`/`catch` — a *thrown* exception (the admin rate limiter, a transient DB hiccup, a stale session) left the UI silently stuck with nothing saved and no error shown, distinct from the already-handled case of a Server Action returning `{ok: false}`. Fixed everywhere: every one of these calls now always surfaces a specific error message and always resets its pending/saving state. Whatever Erica's attempt actually hit is unrecoverable — she'll need to retry once this deploys — but a repeat should now show a real, actionable error instead of silently doing nothing. Full check suite (`typecheck`/`lint`/`test` — 15 tests/`test:e2e` — 8 tests) passes.
- **2026-07-08** — Jon obtained a PayPal sandbox Client ID/Secret and a Shippo test API key from Erica; both saved to `.env.local` (gitignored). PayPal's Webhook ID and a TaxJar key are still outstanding — the webhook needs feature 3's endpoint to exist first (to register a real callback URL), and TaxJar is still pending from Erica. Per this project's fake-provider testing convention (real credentials are never exercised in automated tests), neither remaining gap blocks starting feature 3 — confirmed with Jon to proceed now.
- **2026-07-08** — TaxJar sandbox key arrived from Erica too, saved to `.env.local`. PayPal's Webhook ID remains the one genuinely outstanding credential — it needs a real, deployed webhook URL to register in PayPal's dashboard before it can exist, so it's deliberately deferred to right before launch, not blocking anything now.
- **2026-07-08** — Implemented feature 3 (cart & checkout), all 33 tasks. An anonymous, cookie-held cart (`docs/adr/0011-client-side-cart-reference.md` — no `carts` table; every read re-fetches and re-prices fresh from canonical data, never trusting a stored price) feeds a checkout flow computing a real TaxJar tax figure, flat or Shippo-calculated shipping (`docs/adr/0012`, `docs/adr/0013`), and at most one promotion (flat/BOGO/promo-code/cart-threshold/free-shipping — greatest-value automatic promotion wins if no code is entered), then pays via PayPal's Orders v2 API called directly via `fetch`, no SDK (`docs/adr/0014-paypal-direct-rest-integration.md`). An order is created as `placed` immediately and is only ever flipped to `paid` by a separately-verified PayPal webhook (`POST /api/webhooks/paypal`) — never by the client-side redirect alone — and the charged total is always recomputed fresh at the moment of payment, never trusted from an earlier checkout-summary call. New `promotions`/`orders`/`order_items` tables (the last as a deliberate frozen JSON snapshot, this project's one exception to typed-relational tables, since a paid order is a historical record that must survive later product/option changes). Every external integration (TaxJar, Shippo, PayPal) ships behind a small interface with a deterministic fake from the start — automated tests never touch real sandbox credentials, gated by a `CHECKOUT_FAKE_PROVIDERS` flag set only by Vitest's setup and Playwright's `webServer.env` (mirroring the existing `E2E_TEST_AUTH` pattern), even though real sandbox keys now sit in `.env.local`. A test-only fake PayPal approval page (`/checkout/fake-paypal-approval`, gated the same way) stands in for PayPal's real hosted approval screen in e2e — and happens to statically bake into a 404 at production build time besides, since the gating flag is never set for a real build. Found and fixed one real cross-feature gap along the way: feature 2's Product Detail page treated "Design Location" as single-select, but feature 1's data model always specified it as multi-select (one or many per product) — fixed before wiring cart's `designLocationOptionIds` array to it, since building checkout on top of the inconsistent single-select UI would have silently lost real product capability. New Vitest coverage: cart re-pricing/availability (`tests/checkout/cart.test.ts`), all 5 promotion types (`tests/checkout/promotions.test.ts`), and order total derivation + webhook idempotency (`tests/checkout/orders.test.ts`) — 38 unit tests total now. A new Playwright e2e (`e2e/cart-checkout.spec.ts`, 3 tests) covers the full browse → add to cart → adjust quantity → checkout with a seeded promo code and calculated shipping → pay via the fake PayPal flow → confirm the order stays `placed` until a deliberately-delayed fake webhook arrives and verifies it `paid`, idempotently on a repeat delivery — with every breakdown figure (subtotal, discount, shipping, tax, total) asserted to the exact cent, plus separate tests for the empty-cart block and an invalid promo code's specific rejection reason. An ad hoc axe scan of the product/cart/checkout pages (empty and populated) found zero violations. Folded the checkout-step-transition target (<1.5s) into `docs/non-functional.md`, and filled in its checkout rate-limit row (30 mutations/min per IP, keyed separately from admin's two-trusted-users tuning). Full check suite (`typecheck`/`lint`/`test` — 38 tests/`test:e2e` — 11 tests) and a production build all pass.

- **2026-07-09** — Before starting feature 4, fixed a real gap Jon found live: the storefront had no link to `/cart` anywhere (not the header nav, not the add-to-cart confirmation) — the page itself worked, but nothing pointed to it. Fixed with a header "Cart" link (a live item-count badge, read cheaply from the cart cookie — no extra DB round trip) and a "View cart" link on the confirmation message.
- **2026-07-09** — Implemented feature 4 (order confirmation), all 15 tasks. A public confirmation page at `/orders/[confirmationToken]` (never `orders.id` — FR-012) shows every line item, shipping address, full breakdown, and a status timeline; while payment is still `placed`, it polls `getOrderConfirmation` every 2s and resolves to `paid` on its own within seconds of the webhook arriving, or shows a "may need attention" message after 60s (FR-004/FR-005). Renders fully dynamically (`force-dynamic`) so a bookmarked/emailed link always reflects current state, never a stale snapshot (FR-010). The instant feature 3's webhook marks an order paid, `sendConfirmationEmail` fires from `src/lib/confirmation/email.ts` — an atomic conditional `UPDATE ... RETURNING` (checks `status = 'paid' AND confirmationEmailSentAt IS NULL` and claims the send in the same operation) guarantees exactly once even under a redelivered webhook (FR-008); a Resend delivery failure is caught and logged, never affecting the order or the page (FR-009). Resend joins the established fake-provider pattern, gated by the same `CHECKOUT_FAKE_PROVIDERS` flag as TaxJar/Shippo/PayPal (`docs/adr/0015-resend-for-transactional-email.md`). Found and fixed two real gaps along the way, both zero-cost since neither had shipped yet: (1) `checkout/return` was still feature 3's placeholder "thank you" text — spec.md called for this feature to replace it, so it now resolves the PayPal-side token to the order's `confirmationToken` and redirects straight to the real confirmation page, with feature 3's own e2e updated to match; (2) `createOrderAndPayment` never cleared the cart cookie after successfully creating an order, so purchased items would linger in the cart indefinitely — fixed with a `writeCart([])` right after the order transaction commits. New Vitest coverage (`tests/confirmation/email.test.ts`, 4 tests) covers the send-once idempotency guard directly against the real local database (a first for this project's business-logic tests — the guarantee is fundamentally about atomic DB behavior, not something a pure function can represent) — sends once for a paid/unsent order, never resends, never sends for a not-yet-paid order, and catches a forced delivery failure without throwing or losing the order's paid status. A new Playwright e2e (`e2e/order-confirmation.spec.ts`, 3 tests) covers the full field set on the confirming state, the confirming-to-paid transition resolving with no page reload (proving the client-side polling actually works, not just the page's static render), a revisit showing the same accurate state, a nonexistent-token not-found case, and that two different orders' pages never cross-expose each other's data. An ad hoc axe scan (confirming, paid, and not-found states) found zero violations. Full check suite (`typecheck`/`lint`/`test` — 42 tests/`test:e2e` — 14 tests) and a production build all pass.

- **2026-07-09** — Verified features 3 and 4 live in Production for the first time (they'd only ever run against local Postgres + fake providers before). Installed and linked the Vercel CLI to unblock this. Found the deploy itself was further behind than expected: **no checkout env var had ever been provisioned in Production** (PayPal, TaxJar, Shippo, Resend, `SHOP_ORIGIN_*` were all missing) and **feature 3's migration had never been applied** to the production Neon database — both fixed (env vars copied from `.env.local` per Jon's confirmation to stay on PayPal sandbox for now; migration applied via the existing idempotent `/api/admin/migrate` endpoint, same as feature 1's). `MIGRATE_SECRET` also turned out to be a stale value mismatched between `.env.local` and Production from initial setup — rotated to a fresh value in both. Three real bugs surfaced only by an actual live checkout attempt, each fixed and redeployed in turn: (1) `tax.ts` always called TaxJar's production API URL, which rejects a sandbox key outright — added `TAXJAR_MODE` (sandbox/live), mirroring `PAYPAL_MODE` exactly; (2) the checkout form's State field was free-text with no validation, so a full state name ("Ohio") reached TaxJar and 500'd with an opaque error — replaced with a `<select>` of all 50 states + DC (`src/lib/us-states.ts`), validated server-side against the same list; (3) **no PayPal webhook subscription had ever been registered** — PayPal's Webhooks API confirmed zero existing subscriptions for the sandbox app, so a completed sandbox payment finalized on PayPal's side but our webhook handler never got called, leaving the order stuck at `placed` forever (this specific test order — `confirmationToken` starting `97fd7c61`— stays stuck; PayPal doesn't retroactively redeliver past events to a newly-created subscription, and no admin override tool exists yet). Registered a real subscription via PayPal's Webhooks API (`POST /v1/notifications/webhooks`, `PAYMENT.CAPTURE.COMPLETED`) pointed at `https://ericaburnsthings.vercel.app/api/webhooks/paypal`, and added the resulting `PAYPAL_WEBHOOK_ID` to `.env.local`/Vercel — this closes the constitution's last outstanding "register a live webhook" follow-up (for sandbox; a live-mode subscription still needs registering at actual launch, see below). A subsequent full sandbox checkout completed correctly end-to-end.

- **2026-07-09** — Implemented feature 5 (admin: orders, discounts, shipping & fees), all 21 tasks — **the last MVP feature; the MVP is now feature-complete end to end.** An order queue (`/admin/orders`) and read-only detail view (`/admin/orders/[id]`) reuse feature 1's Auth.js gate and admin rate limiter directly. Fulfillment status is an explicit, validated state machine (`src/lib/admin/order-status.ts`): `paid` → `in production` → `shipped`, one step at a time — `placed` → `paid` isn't even representable as an admin-settable target (it's outside the map's type, not just runtime-rejected), so the UI can only ever offer the single legal next action. Full CRUD over feature 3's `promotions` table (`/admin/discounts`) — Zod validates shape, then per-type required fields are checked against `src/lib/checkout/promotions.ts`'s actual evaluation logic (not the wireframe's aspirational percentage-discount/usage-limit/free-processing fields, none of which this schema or checkout support); a duplicate promo code is caught via the existing global case-insensitive unique index and surfaced as a clear field error. `deletePromotion` stays deliberately thin — safety is the `ON DELETE SET NULL` FK, not application cleanup. One new table, `shop_settings` (a genuine single-row singleton, `id` fixed at 1) holds the flat-rate shipping amount (`/admin/settings`); `src/lib/checkout/shipping.ts`'s flat branch now reads it, falling back to the existing $7.99 default when unset. Extracted `PriceInput` out of the Product Editor into `src/components/price-input.tsx` so every currency field (product pricing, promotion amounts, shipping settings) shares one typing-safe implementation rather than risking the exact bug fixed once already (`10c6a35`). Found and fixed one real regression while writing the full-vertical-slice e2e test: feature 4's confirmation-page timeline had `in production`/`shipped` *hardcoded* as never-reached, written back when nothing could ever set them — now that this feature can, `buildConfirmationView`/`ConfirmationStatus` were updated to reflect the real status on every fresh load, so a shipped order's confirmation page actually shows "Shipped" as reached. Also caught and fixed a real Next.js build break before it ever reached a test: re-exporting a type (`export type { X }`) from a `"use server"` file crashes Turbopack's action-reference transform at runtime — both new action files had this, fixed by importing types from their source lib modules directly instead. New Vitest coverage: the state-machine's every legal/illegal transition (`tests/admin/order-status.test.ts`, 8 tests) and promotion CRUD — duplicate-code rejection, type-specific field requirements, and deactivate-then-delete leaving a real order's `discountCents`/`promotionId` provably unchanged (`tests/admin/promotions.test.ts`, 3 tests) — both directly against the real local database via the same auth-free lib-layer pattern established for `order-math.ts`/`confirmation/email.ts`, since `requireAdminSession()` depends on `next/headers` and can't run outside a real request. Three new Playwright e2e specs: `admin-orders.spec.ts` (queue, detail, full fulfillment advance, and confirms an unpaid order offers no admin action at all), `admin-discounts.spec.ts` (a promotion created through the real admin UI applies at the very next checkout, then deactivating it correctly blocks it), and `full-vertical-slice.spec.ts` — the one standing happy-path e2e Constitution Principle V has required since ratification (browse → cart → checkout → pay → confirmation → admin queue → fulfillment → confirmation reflects it), finally completable now that every piece it touches exists. An ad hoc axe scan of all four new screens found zero violations; unauthenticated requests to all three new routes confirmed redirecting to sign-in. Full check suite (`typecheck`/`lint`/`test` — 53 tests/`test:e2e` — 17 tests) and a production build all pass.

- **2026-07-09** — Implemented feature 6 (site content pages), all 21 tasks — the first post-MVP feature, following the same specify → plan → tasks → implement cycle as the MVP. The storefront footer now links to Privacy Policy, Terms of Use, and About Us pages (`/privacy`, `/terms`, `/about`); each page's title and rich-formatted body are admin-editable through a Tiptap editor at `/admin/content` (`docs/adr/0017-tiptap-and-sanitize-html-for-rich-content.md`), live on save with no deployment needed — the same pattern as promotions/shipping settings. One new table, `site_pages`, holds only admin-saved overrides; a page with no override yet falls back to a code-level default (`src/lib/admin/site-pages.ts`) — Privacy/Terms default to the existing placeholder content from `Resources/shared/` (faithfully converted, not rewritten — that's still the owner's job, now actually possible without a code change), About defaults to a short placeholder message. Every save is sanitized server-side via `sanitize-html`'s allowlist before it ever reaches the database, so the public pages can safely render it via `dangerouslySetInnerHTML`. Two real bugs found via testing, both fixed before shipping: (1) the sanitizer's `transformTags` was adding `rel`/`target` to external links, but those attributes weren't in the allowlist, so the sanitizer's own filter silently stripped them right back off — external links would have opened same-tab with no `noopener`/`noreferrer` protection; (2) adding the footer's second `<nav>` landmark with no distinguishing label made it indistinguishable from the header nav for screen-reader landmark navigation (a real, not cosmetic, a11y regression) — both header and footer nav now carry an `aria-label`. Also handled a genuine test-isolation problem specific to this feature: unlike every other e2e spec in this project, the three content pages are fixed, non-unique slugs (not a fresh timestamped entity per test), so the "still on its default" and "admin edit" tests would race under Playwright's default parallel execution — fixed with `test.describe.configure({ mode: "serial" })` plus a `beforeAll` cleanup, making the suite idempotently re-runnable. New Vitest coverage (`tests/admin/site-pages.test.ts`, 8 tests) covers sanitization against real XSS payload shapes (`<script>`, `onerror`, `javascript:` href) and the fallback-default/override logic. A new Playwright e2e (`e2e/site-content-pages.spec.ts`, 3 tests) covers the footer→page flow, the unauthenticated-editor-redirects case, and a full admin edit (heading/bold/tab-switch) reflected live on the public page. An ad hoc axe scan of all three public pages and the admin editor found zero violations after the nav-label fix. Full check suite (`typecheck`/`lint`/`test` — 61 tests/`test:e2e` — 20 tests) and a production build all pass. Not yet pushed to Production.

## Next steps

1. Push feature 6 to Production (commit, push, redeploy, verify live, apply the `site_pages` migration) the same way features 3/4/5 were on 2026-07-09.
2. Before actual launch: obtain PayPal's live Client ID/Secret and register a *live-mode* webhook subscription (the sandbox one registered 2026-07-09 only covers testing), TaxJar's live key (and flip `TAXJAR_MODE=live`), and Shippo's live key — all currently sandbox/test only, deliberately deferred. A real `RESEND_API_KEY` is already in place and live-functional.
3. Consider whether the stuck test order from 2026-07-09 (`confirmationToken` starting `97fd7c61`) is worth a manual cleanup now that the admin order queue exists to see it, or just left as harmless test data.
4. Real launch-readiness pass: the owner should rewrite the Privacy/Terms content through the new `/admin/content` editor (still the name-swapped generic template, now actually easy to fix), write real About Us copy, resolve the Recipe Board custom-upload conflict flagged in the Launch Catalog review, and the deferred product customizer / upload-your-own-design fast-follow.
